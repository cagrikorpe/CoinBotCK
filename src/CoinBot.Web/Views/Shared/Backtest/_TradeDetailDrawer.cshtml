<aside class="cb-drawer cb-ai-drawer" id="cb_backtest_trade_drawer" aria-hidden="true">
    <div class="cb-drawer-header">
        <div>
            <div class="cb-drawer-eyebrow">Trade Detail</div>
            <h5 class="cb-drawer-title mb-0" id="cb_backtest_drawer_title">BTCUSDT · Long</h5>
        </div>
        <button type="button" class="btn btn-xs btn-icon btn-light" data-cb-drawer-close>
            <span>&times;</span>
        </button>
    </div>
    <div class="cb-drawer-body">
        <div class="cb-rule-setting-item">
            <div class="cb-rule-setting-header"><strong>İşlem özeti</strong><span class="cb-badge cb-badge-info" id="cb_backtest_drawer_setup">EMA Cross</span></div>
            <div class="cb-ai-chip-list mt-3">
                <span class="cb-badge cb-badge-success" id="cb_backtest_drawer_direction">Long</span>
                <span class="cb-badge cb-badge-neutral" id="cb_backtest_drawer_symbol">BTCUSDT</span>
                <span class="cb-badge cb-badge-warning" id="cb_backtest_drawer_pnl">+3.4%</span>
            </div>
        </div>
        <div class="cb-rule-setting-item mt-4">
            <div class="cb-rule-setting-header"><strong>Neden / setup notu</strong><span class="cb-badge cb-badge-info">Placeholder</span></div>
            <div class="text-muted font-size-sm mt-2" id="cb_backtest_drawer_reason">Trend teyidi ve hacim desteği ile pozisyon açıldı.</div>
        </div>
        <div class="cb-rule-setting-item mt-4">
            <div class="cb-rule-setting-header"><strong>Detay metrik</strong><span class="cb-badge cb-badge-neutral">Sonradan bağlanır</span></div>
            <ul class="mb-0 pl-5 text-muted font-size-sm mt-2">
                <li>Entry/exit sinyal blokları gerçek strategy runtime ile eşlenebilir.</li>
                <li>Trade drawdown, hold time ve fee kırılımı sonraki fazda buraya eklenebilir.</li>
                <li>Backtest ile Paper Trading akışı bilinçli olarak ayrılmıştır.</li>
            </ul>
        </div>
    </div>
    <div class="cb-drawer-footer">
        <button type="button" class="btn btn-light-primary font-weight-bold" data-cb-drawer-close>Kapat</button>
        <button type="button" class="btn btn-primary font-weight-bolder">Sonuç kartı detayı</button>
    </div>
</aside>
