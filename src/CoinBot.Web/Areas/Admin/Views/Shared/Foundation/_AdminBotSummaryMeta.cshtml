@using CoinBot.Application.Abstractions.Administration
@{
    var snapshot = ViewData["AdminBotOperationsPageSnapshot"] as AdminBotOperationsPageSnapshot;
    var tiles = snapshot?.SummaryTiles ?? Array.Empty<AdminStatTileSnapshot>();
}

<div class="cb-admin-summary-grid cb-admin-bot-summary-grid">
    @if (tiles.Count == 0)
    {
        <div class="cb-admin-summary-card">
            <span class="cb-admin-summary-label">Bot snapshot</span>
            <strong>Veri yok</strong>
            <small>Filtre sonucu boş</small>
        </div>
    }
    else
    {
        @foreach (var tile in tiles)
        {
            <div class="cb-admin-summary-card">
                <span class="cb-admin-summary-label">@tile.Label</span>
                <strong class="cb-mono">@tile.Value</strong>
                <small>@tile.Meta</small>
            </div>
        }
    }
</div>
