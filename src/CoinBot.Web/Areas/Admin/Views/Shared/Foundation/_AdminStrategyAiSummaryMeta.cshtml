@using CoinBot.Application.Abstractions.Administration
@{
    var snapshot = ViewData["AdminStrategyAiMonitoringPageSnapshot"] as AdminStrategyAiMonitoringPageSnapshot;
    var tiles = snapshot?.SummaryTiles ?? Array.Empty<AdminStatTileSnapshot>();
}

<div class="cb-admin-summary-grid cb-admin-strategy-ai-summary-grid">
    @if (tiles.Count == 0)
    {
        <div class="cb-admin-summary-card">
            <span class="cb-admin-summary-label">AI snapshot</span>
            <strong>Veri yok</strong>
            <small>Strategy/AI verisi bulunamadı</small>
        </div>
    }
    else
    {
        @foreach (var tile in tiles)
        {
            <div class="cb-admin-summary-card">
                <span class="cb-admin-summary-label">@tile.Label</span>
                <strong>@tile.Value</strong>
                <small>@tile.Meta</small>
            </div>
        }
    }
</div>
