@{
    var headerModel = new AdminPageHeaderViewModel
    {
        Eyebrow = "Platform Alerts",
        Title = "Bildirim / Alarm Merkezi",
        Description = "AI, risk, worker, security ve system alarm sinyallerini unread/read ayrımı, severity filtreleri ve detail drawer ile tek merkezde toplayan Super Admin alarm merkezi.",
        StatusBadge = new AdminBadgeViewModel { Label = "Alert Hub", Tone = "warning", IconText = "AL" },
        MetaBadges = new[]
        {
            new AdminBadgeViewModel { Label = "Unread Aware", Tone = "info", IconText = "UR" },
            new AdminBadgeViewModel { Label = "Severity Filtered", Tone = "critical", IconText = "SV" },
            new AdminBadgeViewModel { Label = "Policy CTA", Tone = "degraded", IconText = "PL" }
        },
        SecondaryActionText = "Audit / log merkezi",
        SecondaryActionHref = Url.Action("Audit", "Admin", new { area = "Admin" }),
        PrimaryActionText = "Security events",
        PrimaryActionHref = Url.Action("SecurityEvents", "Admin", new { area = "Admin" })
    };
}

<div class="row">
    <div class="col-12">
        <div class="card card-custom gutter-b cb-card cb-admin-card">
            <div class="card-body">
                @await Html.PartialAsync("Foundation/_AdminPageHeader", headerModel)
            </div>
        </div>
    </div>

    <div class="col-12">
        @await Html.PartialAsync("Foundation/_AdminAlertSummaryMeta")
    </div>

    <div class="col-12">
        @await Html.PartialAsync("Foundation/_AdminAlertFilterBar")
    </div>

    <div class="col-12">
        @await Html.PartialAsync("Foundation/_AdminAlertCategoryStrip")
    </div>

    <div class="col-xl-8">
        @await Html.PartialAsync("Foundation/_AdminAlertList")
    </div>

    <div class="col-xl-4">
        @await Html.PartialAsync("Foundation/_AdminAlertPolicyCta")
    </div>

    <div class="col-12">
        @await Html.PartialAsync("Foundation/_AdminEmptyState", new UiStateViewModel
        {
            Title = "Alarm merkezi hazır",
            Message = "Alarm listesi monitoring snapshot ve incident feed ile doldurulur. Alarm yoksa empty-state gösterilir.",
            IconText = "AL",
            ActionText = "System health",
            ActionHref = Url.Action("SystemHealth", "Admin", new { area = "Admin" }),
            SecondaryActionText = "Audit / loglar",
            SecondaryActionHref = Url.Action("Audit", "Admin", new { area = "Admin" })
        })
    </div>
</div>
